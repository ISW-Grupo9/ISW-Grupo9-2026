services:
  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"   # SMTP — Spring Boot apunta acá (application.properties)
      - "8025:8025"   # Bandeja de entrada web → http://localhost:8025
